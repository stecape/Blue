# Host address for the React development server
HOST=0.0.0.0

# Port for the React development server
PORT=3000

# Enable hot reload with polling (needed for Docker on Windows)
CHOKIDAR_USEPOLLING=true
WATCHPACK_POLLING=true
# Polling interval in milliseconds (default is 2000ms, lower = faster but more CPU)
CHOKIDAR_INTERVAL=300
WATCHPACK_POLLING_INTERVAL=300

# WebSocket configuration for HMR through nginx
WDS_SOCKET_HOST=0.0.0.0
WDS_SOCKET_PORT=80
WDS_SOCKET_PATH=/ws

# Backend server IP address (commented out to use dynamic detection)
# REACT_APP_SERVER_IP=http://localhost

# Server API URL for authentication
REACT_APP_API_URL=https://www.stecape.space